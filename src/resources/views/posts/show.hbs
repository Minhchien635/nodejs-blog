<div class="mt-4">
    <div class="container text-center">
            <p class="text-left">{{username}}</p>
            <h2>{{post.title}}</h2>         
            <p>{{{post.content}}}</p>
    </div>
</div>